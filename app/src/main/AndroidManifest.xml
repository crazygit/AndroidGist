<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.github.crazygit.gist">

    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.CameraXDemo"></activity>
        <activity android:name=".activities.TestUI" />
        <activity android:name=".activities.AnimatorSetDemo" />
        <activity android:name=".activities.AnimatorDemo" />
        <activity android:name=".activities.JetpackLiveDataDemo" />
        <activity android:name=".activities.Jetpack" />
        <activity android:name=".activities.CustomCircularProgressBar" />
        <activity android:name=".activities.AnimationsAndTransitions" />
        <activity android:name=".activities.UserInterfaceAndNavigation" />
        <activity android:name=".activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>